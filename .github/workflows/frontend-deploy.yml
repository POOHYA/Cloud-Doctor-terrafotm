name: Frontend Deploy to Private Subnet

on:
  # 8:40 워크플로
  schedule:
    - cron: "40 23 * * *"
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "frontend/**"

# 1️⃣ OIDC 권한 설정
permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 2️⃣ 코드 체크아웃
      - name: Checkout repository
        uses: actions/checkout@v3

      # 3️⃣ AWS OIDC 인증
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-northeast-2

      # 4️⃣ Node.js 환경 설정
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      # 5️⃣ 패키지 설치 및 빌드
      - name: Install dependencies and build
        working-directory: frontend/cloud-doctor
        env:
          REACT_APP_API_URL: ${{ secrets.REACT_APP_API_URL }}
          REACT_APP_AUDIT_API_URL: ${{ secrets.REACT_APP_AUDIT_API_URL }}
        run: |
          npm ci
          CI=false npm run build

      # 6️⃣ 빌드 파일 압축 (./build 안의 내용만 압축)
      - name: Create build archive
        run: |
          cd frontend/cloud-doctor/build
          tar -czf ../../../build.tar.gz .

      # 7️⃣ Bastion 서버로 빌드 파일 및 Private Key 업로드
      - name: Copy build files to Bastion
        uses: appleboy/scp-action@v0.1.5
        with:
          host: ${{ secrets.BASTION_PUBLIC_IP }}
          username: ec2-user
          key: ${{ secrets.BASTION_SSH_KEY }}
          source: "build.tar.gz"
          target: "/home/ec2-user/"

      - name: Copy Private Key to Bastion
        uses: appleboy/scp-action@v0.1.5
        with:
          host: ${{ secrets.BASTION_PUBLIC_IP }}
          username: ec2-user
          key: ${{ secrets.BASTION_SSH_KEY }}
          source: "infra/terraform/clouddoctor-key.pem"
          target: "/home/ec2-user/private-cloud-doctor.pem"
          strip_components: 2

      # 8️⃣ ASG 스케일업 및 인스턴스 준비 대기
      - name: Scale up ASG and wait for instances
        run: |
          ASG_NAME="${{ secrets.FE_ASG_NAME }}"

          echo "🔄 ASG 스케일업 시작..."
          aws autoscaling set-desired-capacity \
            --auto-scaling-group-name "$ASG_NAME" \
            --desired-capacity 2 \
            --region ap-northeast-2

          echo "⏳ 인스턴스가 InService 상태가 될 때까지 대기 중..."
          for i in {1..20}; do
            INSTANCE_COUNT=$(aws autoscaling describe-auto-scaling-groups \
              --auto-scaling-group-names "$ASG_NAME" \
              --region ap-northeast-2 \
              --query 'AutoScalingGroups[0].Instances[?LifecycleState==`InService`].InstanceId' \
              --output text | wc -w)
            
            if [ "$INSTANCE_COUNT" -ge 1 ]; then
              echo "✅ $INSTANCE_COUNT 개의 인스턴스가 준비되었습니다."
              break
            fi
            
            echo "대기 중... ($i/20)"
            sleep 30
          done

      # 9️⃣ Bastion에서 실시간 ASG 인스턴스 IP 조회 및 순차 배포
      - name: Deploy to Dynamic ASG Instances via Bastion
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.BASTION_PUBLIC_IP }}
          username: ec2-user
          key: ${{ secrets.BASTION_SSH_KEY }}
          script: |
            chmod 400 /home/ec2-user/private-cloud-doctor.pem

            ASG_NAME="${{ secrets.FE_ASG_NAME }}"
            echo "🔍 ASG에서 인스턴스 조회 중..."

            INSTANCE_IDS=$(aws autoscaling describe-auto-scaling-groups \
              --auto-scaling-group-names "$ASG_NAME" \
              --region ap-northeast-2 \
              --query 'AutoScalingGroups[0].Instances[?LifecycleState==`InService`].InstanceId' \
              --output text 2>&1)

            echo "DEBUG: INSTANCE_IDS = $INSTANCE_IDS"

            if [ -z "$INSTANCE_IDS" ] || [ "$INSTANCE_IDS" == "None" ]; then
              echo "❌ 인스턴스를 찾을 수 없습니다."
              exit 1
            fi

            SERVER_IPS=$(aws ec2 describe-instances \
              --instance-ids $INSTANCE_IDS \
              --region ap-northeast-2 \
              --query 'Reservations[*].Instances[*].PrivateIpAddress' \
              --output text 2>&1)

            echo "DEBUG: SERVER_IPS = $SERVER_IPS"
            echo "📍 배포 대상 IP: $SERVER_IPS"

            for IP in $SERVER_IPS; do
              echo "------------------------------------------------"
              echo "🚀 배포 시작: $IP"
              echo "------------------------------------------------"
              
              scp -i /home/ec2-user/private-cloud-doctor.pem -o StrictHostKeyChecking=no /home/ec2-user/build.tar.gz ec2-user@$IP:/home/ec2-user/
              
              ssh -i /home/ec2-user/private-cloud-doctor.pem -o StrictHostKeyChecking=no ec2-user@$IP << 'EOF'
            mkdir -p ~/temp_build
            tar -xzf ~/build.tar.gz -C ~/temp_build/
            sudo rm -rf /var/www/html/*
            sudo cp -r ~/temp_build/* /var/www/html/
            sudo chown -R nginx:nginx /var/www/html
            sudo chmod -R 755 /var/www/html
            sudo systemctl reload nginx
            rm -f ~/build.tar.gz
            rm -rf ~/temp_build
            EOF
              echo "✅ 배포 성공: $IP"
              sleep 5
            done

      # 🔟 배포 완료 후 ASG 스케일다운
      - name: Scale down ASG after deployment
        if: always()
        run: |
          ASG_NAME="${{ secrets.FE_ASG_NAME }}"
          echo "🔽 배포 완료 - ASG 스케일다운 시작..."
          aws autoscaling set-desired-capacity \
            --auto-scaling-group-name "$ASG_NAME" \
            --desired-capacity 0 \
            --region ap-northeast-2
          echo "✅ ASG 스케일다운 완료"
